const { expect } = require("chai");
const sinon = require("sinon");

const userRoutes = require("../dist/routes/user");

describe("User routes", () => {
  it("should error if no auth bearer token", () => {});
  it("should error if length of order is 0", () => {});
});
